// Injected by vite's `define` at build time (see vite.config.ts). Shown in the ?diag=1 panel so
// a phone can say which bundle it is running — "is it the new build?" cost several round trips of
// remote testing before this existed, and a stale cache is indistinguishable from a real bug.
declare const __VE_BUILD__: string;
